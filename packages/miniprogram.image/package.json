{
  "name": "@doraemon-ui/miniprogram.image",
  "private": false,
  "version": "0.0.1-alpha.0",
  "description": "miniprogram image component for doraemon-ui",
  "author": "skyvow",
  "homepage": "",
  "license": "MIT",
  "miniprogram": "miniprogram_dist",
  "main": "miniprogram_dist/index.js",
  "module": "miniprogram_dist/index.js",
  "devMain": "src/index.ts",
  "types": "index.d.ts",
  "keywords": [
    "doraemon-ui",
    "miniprogram",
    "image"
  ],
  "scripts": {
    "serve": "dora serve ./playground --open --build-npm",
    "build:dev": "dora build --watch",
    "build": "dora build",
    "test": "cross-env NODE_ENV=testing jest --coverage",
    "clean": "cross-env rimraf miniprogram_dist/* .coverage-report/*",
    "preversion": "pnpm clean && pnpm build && pnpm test"
  },
  "dependencies": {
    "@doraemon-ui/miniprogram.core-js": "workspace:*",
    "@doraemon-ui/style": "workspace:*"
  },
  "devDependencies": {
    "@doraemon-ui/miniprogram.cli": "0.0.2-alpha.55",
    "@types/node": "^25.5.2",
    "cross-env": "^7.0.3",
    "jest": "^30.3.0",
    "rimraf": "^3.0.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
