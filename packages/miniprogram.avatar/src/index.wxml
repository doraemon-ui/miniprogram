<view class="dora-class {{ classes.wrap }}" style="{{ extStyle }}">
    <image src="{{ src }}" wx:if="{{ src }}" />
    <text class="{{ classes.string }}" style="{{ childrenStyle }}" wx:else>
        <slot></slot>
    </text>
</view>
