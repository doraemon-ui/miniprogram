{
  "name": "@doraemon-ui/miniprogram.demo-block",
  "private": false,
  "version": "0.0.1-alpha.2",
  "description": "miniprogram demo-block component for doraemon-ui",
  "author": "skyvow",
  "homepage": "",
  "license": "MIT",
  "miniprogram": "miniprogram_dist",
  "main": "miniprogram_dist/index.js",
  "module": "miniprogram_dist/index.js",
  "devMain": "src/index.ts",
  "types": "index.d.ts",
  "keywords": [
    "doraemon-ui",
    "miniprogram",
    "demo-block"
  ],
  "scripts": {
    "serve": "dora serve ./proscenium --open --build-npm",
    "build:dev": "dora build --watch",
    "build": "dora build",
    "test": "cross-env NODE_ENV=testing jest --coverage",
    "clean": "cross-env rimraf miniprogram_dist/* .coverage-report/*",
    "preversion": "npm run clean && npm run build && npm run test"
  },
  "dependencies": {
    "@doraemon-ui/miniprogram.core-js": "^0.0.2-alpha.21",
    "@doraemon-ui/style": "^0.0.1-alpha.6"
  },
  "devDependencies": {
    "@doraemon-ui/cli": "^0.0.2-alpha.35",
    "cross-env": "^7.0.3",
    "jest": "^27.0.6",
    "rimraf": "^3.0.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
